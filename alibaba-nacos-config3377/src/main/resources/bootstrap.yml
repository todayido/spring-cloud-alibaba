server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:1111
      config:
        server-addr: localhost:1111
        file-extension: yaml
        group: TEST_GROUP